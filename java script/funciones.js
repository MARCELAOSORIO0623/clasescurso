//explicada
/*function sumatres(x) {
    var sum= (x + 3);
    console.log(sum);
    }
sumatres(9);

// en real seria 
function sumatres(x) {
    var sum= (x + 3);
    return sum;
    console.log(sum);
}
sumatres(9);

//otra forma de definir una funcion
var sumatres= function(x) {
return x + 3;
}

//otra forma
var sumatres= (x) => {
    return x + 3;
}*/

/*consol log; nos da el valor de una variable o lo que devuelve una linea de ocodigo
solo se utiliza como ayuda, el return si es un reotrnador de valor de una funcion


function consolog(nombre){
    console.log(nombre);
    return nombre;
}

asi se ejecuta la anterior funcion en node > 
function consolog(nombre){
...     console.log(nombre);
...     return nombre;
... }
undefined
> consolog
[Function: consolog]
> consolog("camilo");
camilo   //sale porque la consola nos muestra esa variable
'camilo'  // sale por el return del dato que se ingreso y la operacion que se realiza en la funcion 

function otrafuncion() {
    return ("nombre retornado por la funcion 'consolog' es: " + consolog('camilo'));
    } //una funcion dentro de otra funcion
    // todo lo q esta debajo del return no se ejecutara hasta ahi llega la orden
    */


